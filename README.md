# Add to Steam for GNOME
An extension that allows you to add executables to steam without having to restart the client, just like SteamOS.

This extension requires a `steamos-add-to-steam` binary to be present on your system.

## Development
- Open Nested GNOME Shell: `just run-nested-shell`
- Recompile schema~~s~~: `just recompile-schemas`